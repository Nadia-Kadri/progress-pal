import { createTheme } from '@mui/material/styles';

const theme = createTheme({
  palette: {
    background: {
      default: '#f8f9fb'
    },
    primary: {
      main: '#FF5722'
    },
    secondary: {
      main: '#22CAFF'
    },
  },
});

export default theme;